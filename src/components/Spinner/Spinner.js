import React from 'react';
import { BarLoader } from 'react-spinners';

const Spinner = () => {
  return (
    <div className="cotainer">
      <BarLoader color={'#f89c1d'} />
    </div>
  )
}

export default Spinner;